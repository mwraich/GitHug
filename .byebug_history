exit
 Profile.pref_search(Profile.find(user).partner.tohash).where.not(id: self.id)
 Profile.pref_search(Profile.find(user).partner.tohash).reject {|x| x if x.id == self.id }
 Profile.pref_search(Profile.find(user).partner.tohash).first
 Profile.pref_search(Profile.find(user).partner.tohash).last
 Profile.pref_search(Profile.find(user).partner.tohash)
self
exit
a
self
a
a.delete(self)
a.map {|x| x.first_name}
a.pluck(:first_name)
a.pluck(:name)
a.first.class
self
self.profile
self
a.first.class
a.first
a = Profile.pref_search(Profile.find(user).partner.tohash)
